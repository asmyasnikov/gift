// Пример конфигурации проекта
// Скопируйте этот файл в config.js и настройте под свой проект

export const config = {
  // Заголовок сайта
  title: "С днём рождения, мамочка!",
  
  // Настройки для деплоя на Vercel
  vercel: {
    // Имя проекта на Vercel (если отличается от имени репозитория)
    // Если null, будет использоваться имя из package.json или имя репозитория
    projectName: null,
    // Доменное имя (если используется кастомный домен)
    // Если null, будет использоваться стандартный домен Vercel
    domain: null,
  }
};

// Экспортируем отдельные значения для удобства
export const { title } = config;

