# Фото Мозаика

Интерактивное веб-приложение для создания мозаики из фотографий. Проект позволяет создавать красивые мозаики, где основное фото формируется из множества маленьких фотографий-тайлов.

## Возможности

- 🎨 Автоматическое создание мозаики из фотографий
- 🖼️ Слайд-шоу с несколькими главными фото
- 🎭 Поддержка масок для выделения важных областей
- 📱 Адаптивный дизайн для мобильных устройств
- ⚡ Оптимизированная загрузка изображений
- 🖨️ Экспорт в высоком разрешении для печати

## Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd gift
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Подготовьте фотографии:**
   - Поместите все фото в папку `photos/`
   - PNG файлы - это главные фото (сами являются масками)
   - JPG/JPEG файлы - это тайлы для мозаики
   - Запустите индексацию: `npm run index-photos`

4. **Настройте проект:**
   - Откройте `config.js` в корне репозитория
   - Измените заголовок сайта

5. **Запустите локальный сервер:**
   ```bash
   npm run dev
   ```

## Установка и настройка

### Требования

- Node.js 16+ и npm
- Фотографии в формате JPG/JPEG (для тайлов) и PNG (для главных фото)

### Структура проекта

```
gift/
├── photos/          # Все фотографии (PNG - главные фото, JPG/JPEG - тайлы)
├── src/             # Исходный код приложения
├── scripts/         # Скрипты для обработки фотографий
├── config.js        # Конфигурация проекта
└── vercel.json      # Настройки деплоя на Vercel
```

### Подготовка фотографий

1. **Поместите фотографии в папку `photos/`:**
   - **PNG файлы** - это главные фото (сами являются масками для отображения тайлов)
   - **JPG/JPEG файлы** - это тайлы для мозаики
   - Рекомендуется использовать фотографии хорошего качества

2. **Создайте индекс фотографий:**
   ```bash
   npm run index-photos
   ```
   Этот скрипт создаст файл `photos/index.json` с метаданными всех фотографий (включая PNG).

## Настройка проекта для разных сайтов

Проект легко настраивается для использования на разных сайтах. Для этого нужно изменить конфигурационный файл `config.js` в корне репозитория.

### Настройка заголовка

Измените значение `title` в файле `config.js`:

```javascript
export const config = {
  title: "Ваш заголовок здесь",
  // ...
};
```

### Настройка деплоя на Vercel

Проект использует стандартную конфигурацию Vercel из файла `vercel.json`.

**Настройка имени проекта:**
- Имя проекта на Vercel настраивается через веб-интерфейс Vercel или через CLI команду `vercel link`
- В `config.js` можно указать `projectName` для справки

**Настройка домена:**
- Кастомный домен настраивается через веб-интерфейс Vercel в настройках проекта
- В `config.js` можно указать `domain` для справки

### Примеры конфигурации

**День рождения:**
```javascript
export const config = {
  title: "С днем рождения, мамочка!",
  vercel: {
    projectName: null,
    domain: null,
  }
};
```

**Юбилей:**
```javascript
export const config = {
  title: "С юбилеем!",
  vercel: {
    projectName: "anniversary-mosaic",
    domain: null,
  }
};
```

## Создание главных фото

### Автоматическое определение главных фото

Главные фото определяются **автоматически** по расширению файла - все **PNG файлы** являются главными фото.

**Пример:**
- Главное фото: `main1.png` (PNG файл)
- Тайлы: `photo1.jpg`, `photo2.jpeg` (JPG/JPEG файлы)

### Формат главных фото

**Формат файла:**
- **Формат:** PNG (с поддержкой альфа-канала)
- **Расширение:** `.png`
- **Назначение:** PNG файл сам является маской для отображения тайлов

**Логика отображения тайлов:**
- **Прозрачные области PNG** (альфа = 0) = тайлы **видны**
- **Непрозрачные области PNG** (альфа = 255) = тайлы **скрыты**

**Важно:** PNG файл одновременно является и главным фото, и маской для тайлов.

### Как создать главное фото

#### Вариант 1: Photoshop / GIMP
1. Откройте исходное фото
2. Создайте маску с прозрачными областями там, где должны быть видны тайлы
3. **Закрасьте ПРОЗРАЧНЫМ** области, где тайлы должны быть видны
4. Оставьте **непрозрачными** области, где должно быть видно главное фото
5. Используйте мягкие кисти для плавного перехода
6. Экспортируйте как PNG с альфа-каналом

#### Вариант 2: Онлайн редакторы
- Используйте любой редактор, поддерживающий альфа-канал PNG
- Создайте изображение с прозрачными областями для тайлов

#### Вариант 3: Автоматическая генерация
- Можно использовать инструменты для выделения объектов (например, Remove.bg API)
- Удалите фон или ненужные части, оставив только важные области непрозрачными

### Пример структуры файлов

```
photos/
  ├── main1.png       (главное фото - PNG файл)
  ├── main2.png       (главное фото - PNG файл)
  ├── photo1.jpg      (тайл - JPG файл)
  ├── photo2.jpeg     (тайл - JPEG файл)
  └── index.json      (индекс всех фотографий)
```

### Важные моменты

1. **Плавные переходы:** Используйте размытие (blur) или мягкие кисти на краях прозрачных областей
2. **Размер:** PNG файл должен быть того же размера или пропорционален области отображения
3. **Расположение:** Все файлы должны находиться в папке `/photos/`
4. **Множественные области:** Можно создать несколько прозрачных областей для разных частей фото

## Настройка фильтров

### brightness (яркость)
- **Текущее значение:** 0.85 (85% от оригинальной яркости)
- **Назначение:** Затемняет тайлы, чтобы основное фото было лучше видно
- **Диапазон:** 0.0 (черный) - 1.0 (оригинал) - >1.0 (ярче)
- **Рекомендации:** 0.7-0.9 для хорошей видимости основного фото

### saturate (насыщенность)
- **Текущее значение:** 1.15 (115% от оригинальной насыщенности)
- **Назначение:** Увеличивает насыщенность цветов тайлов
- **Диапазон:** 0.0 (ч/б) - 1.0 (оригинал) - >1.0 (ярче)
- **Рекомендации:** 1.0-1.2 для естественного вида

Константы находятся в начале файла `src/App.jsx`:
```javascript
const IMAGE_BRIGHTNESS = 0.85; // Измените здесь
const IMAGE_SATURATE = 1.15;   // Измените здесь
```

## Деплой

### Деплой на Vercel (рекомендуется)

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Деплой:**
   ```bash
   npm run deploy
   ```

   Или используйте веб-интерфейс Vercel:
   - Подключите репозиторий к Vercel
   - Vercel автоматически определит настройки из `vercel.json`

### Деплой на GitHub Pages

Папка `photos/` добавлена в `.gitignore` и не коммитится в репозиторий по умолчанию.

**Вариант 1: Временное добавление фото в git (рекомендуется)**

1. Убедитесь, что папка `photos/` существует локально
2. Запустите индексацию: `npm run index-photos`
3. Временно добавьте фото в git для деплоя:
   ```bash
   git add -f photos/
   git commit -m "Add photos for deployment"
   git push
   ```

**Вариант 2: Использование Git LFS (для больших файлов)**

1. Установите Git LFS: `git lfs install`
2. Добавьте фото в LFS:
   ```bash
   git lfs track "photos/**"
   git add .gitattributes
   git add photos/
   git commit -m "Add photos via Git LFS"
   git push
   ```

**Настройка GitHub Pages:**

1. Перейдите в Settings → Pages
2. Source: выберите "GitHub Actions"
3. Сохраните изменения

После первого деплоя сайт будет доступен по адресу:
`https://<username>.github.io/<repository-name>/`

## Скрипты

- `npm run dev` - Запуск локального сервера разработки
- `npm run build` - Сборка проекта для продакшена
- `npm run preview` - Предпросмотр собранного проекта
- `npm run index-photos` - Создание индекса фотографий (включая PNG файлы)
- `npm run deploy` - Сборка и деплой на Vercel

## Использование

### Локальный запуск

1. Запустите сервер разработки: `npm run dev`
2. Откройте браузер по адресу `http://localhost:3000`

### Параметры URL

- `?play=1` - Включить автопроигрывание слайд-шоу
- `?debug=1` - Включить режим отладки
- `?photo=0` - Открыть конкретное фото (индекс начинается с 0)

### Управление

- **Стрелки влево/вправо** - Переключение между фото
- **Клик на индикатор** - Переход к конкретному фото
- **Наведение на тайл** - Увеличение тайла
- **Кнопка ⬇️** - Скачать мозаику в высоком разрешении

## Структура для нового проекта

Для создания нового проекта:

1. Откройте `config.js` и измените заголовок
2. Замените фотографии в папке `photos/`:
   - PNG файлы - главные фото (сами являются масками)
   - JPG/JPEG файлы - тайлы для мозаики
3. Запустите `npm run index-photos` для обновления индекса
4. Деплой: `npm run deploy` или через веб-интерфейс Vercel

Папка `photos/` содержит все фотографии:
- PNG файлы - главные фото (сами являются масками для отображения тайлов)
- JPG/JPEG файлы - тайлы для мозаики

## Технологии

- React 18
- Vite
- Sharp (для обработки изображений)
- Canvas API (для генерации мозаики)

## Лицензия

Private project

